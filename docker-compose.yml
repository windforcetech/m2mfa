version: '2'
services:
  web:
    image: m2micro/m2mfa
    build:
      context: .
      args:
        - JAR_FILE=target/m2mfa-2.0.0-SNAPSHOT.jar
      dockerfile: Dockerfile
    ports:
      - 8081:8081